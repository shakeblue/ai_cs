# 화장품 상담 시스템 크롤러 의존성

# 웹 크롤링
requests==2.31.0
beautifulsoup4==4.12.2
lxml==4.9.3
selenium==4.15.2
webdriver-manager==4.0.1

# Playwright (Anti-Detection Browser Automation)
playwright==1.57.0
playwright-stealth==2.0.0

# 데이터베이스
psycopg2-binary==2.9.9
sqlalchemy==2.0.23

# 스케줄링
schedule==1.2.1
python-crontab==3.0.0

# 유틸리티
python-dotenv==1.0.0
pytz==2023.3
python-dateutil==2.8.2

# 로깅 및 모니터링
colorlog==6.8.0

# HTTP 클라이언트
httpx==0.25.2

# HTML 파싱
html5lib==1.1

# 데이터 처리
pandas==2.1.4

# UUID 생성
uuid==1.30

# Supabase 클라이언트
supabase==2.3.4

# OCR (Optional - for local OCR processing)
# Note: PaddleOCR is optional. The pipeline uses OCR.space API by default.
# Uncomment below lines if you want to use local OCR:
# paddlepaddle==3.2.2
# paddleocr==3.3.2

# NLP/Semantic Extraction (Strategy 2)
transformers==4.57.3
torch==2.9.1
sentence-transformers==5.2.0
scikit-learn==1.8.0

# LLM-Based Extraction (Strategy 3)
anthropic==0.40.0  # Claude Haiku 3.5
openai==1.59.5  # GPT-4o Mini
google-generativeai==0.8.3  # Gemini Flash
