# Supabase Configuration for Broadcast Crawler (Phase 3)
# Copy this file to .env and fill in your actual values

# Supabase Project URL
# Find this in your Supabase project settings -> API
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase API Key
# Use SERVICE_KEY for server-side operations (has full permissions)
# Find this in your Supabase project settings -> API -> service_role key
SUPABASE_SERVICE_KEY=your_service_role_key_here

# Optional: Anon key (for client-side operations, if needed)
# This is safe to expose in client-side code
# Find this in your Supabase project settings -> API -> anon/public key
SUPABASE_ANON_KEY=your_anon_key_here

# Note: The persistence layer will use SERVICE_KEY if available,
# otherwise it will fallback to ANON_KEY.
# For saving broadcasts, SERVICE_KEY is recommended.
